extends ../layout

block content
    #title
        | Category
    #button-add-delete
        a.btn.btn-primary.btn-lg.active(href='./categorys/add') Add
        // a.page-link(href='#') Delete
    #main
        table.table
            thead
                tr
                    th(scope='col') #
                    th(scope='col') Name
                    //- th(scope='col') Show/Hidden
                    th(scope='col') Edit/Delete
            tbody
                if result
                     each item in result
                        tr
                            th(scope='row')= item.serial
                            td= item.name
                            //- td
                            //-     a(href= './categorys/' + item._id)= item.hidden ? 'hidden' : 'show'
                                                    
                            td
                                a.btn.btn-primary.btn-lg.active(href= './categorys/edit/' + item._id, role='button', aria-pressed='true') Edit
                                a.btn.btn-primary.btn-lg.active(href= './categorys/delete/' + item._id, role='button', aria-pressed='true') Delete
        if numPage > 1
            nav(aria-label='Page navigation example')
                ul.pagination
                    if ( (page - 1) > 0)
                        li.page-item
                            a.page-link(href=  page > 1 ? './categorys?page=' + (page - 1) : './categorys?page=1', aria-label='Previous')
                                span(aria-hidden='true') &laquo;
                                span.sr-only Previous
                    - for (var i = 1; i <= numPage; i++)
                            if (i == page)
                                li.page-item.active
                                    span.page-link
                                        = page
                                        span.sr-only (current)
                            else
                                li.page-item
                                    a.page-link(href= './categorys?page=' + i)= i
                    if ( (page + 1) <= numPage)
                        li.page-item
                            a.page-link(href= './categorys?page=' + (page + 1), aria-label='Next')
                                span(aria-hidden='true') &raquo;
                                span.sr-only Next